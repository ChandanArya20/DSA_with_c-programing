#include"header.h"
int main()
{
    system("cls");
    char str[]="-987.53";
    // char str1[]="painter";
    // char str2[]="painter";
    // printf("%d",strlen(str));
    // toUpperString(str);
    // toLowerString(str);
    // printf("%d",countVowel(str));
    // printf("%d",countConsonant(str));
    // printf("%d",wordCount(str));
    // reverseStr(str);
    // printf("%d",strcpr(str1,str2));
    // printf("%d",strPelindrum(str));
    // deleteChar(str, 'y');
    // printf("%d",str_to_i(str));
    printf("%lf",str_to_d(str));




    printf("\n%s",str);
    return 0;
}
